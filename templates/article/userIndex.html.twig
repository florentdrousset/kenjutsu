{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
            <h2 class="display-4">Liste des articles</h1>
            <hr class="my-4">
                {% for a in articles %}
                    <h3>{{ a.title }}</h3>
                    <p>{{ a.content }}</p>
                {% endfor %}
    </div>
{% endblock %}